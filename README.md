# Система управления личными финансами

Веб-приложение для учёта личных доходов и расходов, планирования бюджета и анализа финансовой активности. Разработано на Django.

## 📜 Описание

Программа «Система управления личными финансами» предназначена для автоматизации учёта личных финансов. Она позволяет пользователям регистрировать доходы и расходы, классифицировать их по категориям, устанавливать ежемесячные бюджеты и отслеживать их исполнение.

Цель проекта — предоставить удобный и безопасный инструмент для повышения финансовой грамотности и контроля над личным бюджетом.

## ✨ Основные функции

- **Аутентификация пользователей**: Регистрация и вход для разделения данных между пользователями.
- **Управление транзакциями**: Добавление, просмотр, фильтрация и удаление финансовых операций.
- **Пользовательские категории**: Создание собственных категорий для доходов и расходов.
- **Планирование бюджета**: Установка месячных лимитов расходов по категориям.
- **Финансовые отчёты**: Визуализация данных с помощью диаграмм (структура расходов) и таблиц (сравнение с бюджетом).
- **Импорт и экспорт**: Возможность выгружать данные в CSV и загружать их обратно.

## 🛠️ Стек технологий

- **Бэкенд**: Python, Django
- **База данных**: SQLite (по умолчанию)
- **Фронтенд**: HTML, CSS, JavaScript
- **Библиотеки**: `python-dateutil`, `django-environ`

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <URL репозитория>
cd kursach_5
```

### 2. Создание и активация виртуального окружения

```bash
# Для Windows
python -m venv venv
venv\Scripts\activate

# Для macOS/Linux
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Применение миграций базы данных

```bash
python personal_finance/manage.py migrate
```

### 5. Создание суперпользователя

Для доступа к административной панели и входа в систему создайте администратора:

```bash
python personal_finance/manage.py createsuperuser
```
*Следуйте инструкциям в консоли, чтобы задать имя, email и пароль.*

### 6. Запуск сервера разработки

```bash
python personal_finance/manage.py runserver
```

Приложение будет доступно по адресу [http://127.0.0.1:8000/](http://127.0.0.1:8000/). Для входа используйте данные суперпользователя.

## 🗂️ Структура проекта

- `personal_finance/` — основной пакет проекта.
  - `core/` — ключевое приложение, содержащее всю бизнес-логику:
    - `models.py`: Модели базы данных (`Category`, `Transaction`, `Budget`).
    - `views.py`: Логика обработки HTTP-запросов.
    - `forms.py`: Формы для ввода данных.
    - `services.py`: Слой бизнес-логики (расчёты, импорт/экспорт).
    - `urls.py`: Маршруты приложения.
    - `templates/`: HTML-шаблоны.
  - `personal_finance/`: Конфигурация проекта Django (`settings.py`, `urls.py`).
- `docs/` — документация по проекту.
- `requirements.txt` — список зависимостей.

## 🗄️ Схема базы данных

Программа использует три основные модели:

1.  **`Category` (Категория)**
    - `name`: Название категории (например, "Зарплата", "Продукты").
    - `type`: Тип ("Доход" или "Расход").
    - `user`: Привязка к пользователю.

2.  **`Transaction` (Транзакция)**
    - `amount`: Сумма операции.
    - `category`: Связь с категорией.
    - `description`: Необязательное описание.
    - `date`: Дата совершения операции.
    - `user`: Привязка к пользователю.

3.  **`Budget` (Бюджет)**
    - `amount`: Лимит расходов на месяц.
    - `category`: Категория расхода, для которой установлен бюджет.
    - `month`: Месяц, на который действует бюджет.
    - `user`: Привязка к пользователю.
